# Phase 1 (Python) - data preparation and exploratory analysis
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12

# Phase 2 (R) - DLNM models.
# These are R packages, not pip packages, so they are not installed by
# `pip install -r requirements.txt`. Install them once from an R console:
#   install.packages(c("dlnm", "MASS", "dplyr", "ggplot2", "readr", "broom"))
# (the "splines" package ships with base R)
